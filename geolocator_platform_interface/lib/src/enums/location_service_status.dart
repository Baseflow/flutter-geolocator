/// Represents the possible Location Service Statuses
enum LocationServiceStatus {
  /// Location Services are enabled on the native device
  enabled,

  /// Location Servies are disabled on the native device
  disabled
}
